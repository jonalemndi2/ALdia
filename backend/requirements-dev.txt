# ─────────────────────────────────────────────────────────────────────────────
# Dependencias para DESARROLLAR y correr las pruebas.
# Quien solo va a usar el sistema en su comercio no necesita nada de esto: le
# alcanza con `./instalar.sh` (Linux y macOS) o `instalar.bat` (Windows), que
# usan requirements.txt.
#
#     python -m pip install -r backend/requirements-dev.txt
#     python -m pytest tests/ -q
# ─────────────────────────────────────────────────────────────────────────────

# Todo lo que necesita el sistema para funcionar.
-r requirements.txt

# pytest: corre la suite de tests/.
pytest>=8.0
# httpx ya viene por requirements.txt (lo necesita
# backend/routers/pendientes.py en producción). Acá lo usa además el TestClient
# de FastAPI para pegarle a la aplicación montada en memoria, sin levantar el
# servidor.

# mcp: el SDK del servidor MCP. Hace falta para poder PROBAR mcp/aldia_mcp/,
# que hasta ahora no tenia ninguna prueba pese a ser la puerta por la que entran
# las operaciones de un agente. No es una dependencia del backend.
#
# Ojo con una trampa: el repositorio tiene una carpeta llamada `mcp/`, que sin
# el SDK instalado Python toma como paquete de espacio de nombres y tapa al
# verdadero. Un paquete real tiene precedencia, asi que instalarlo lo resuelve.
mcp>=1.2.0
#
# Si NO esta instalado, las pruebas de mcp/ se saltean en vez de fallar: el job
# de CI que verifica el lock instala solo lo que instala un comercio, y romperlo
# por una prueba seria confundir dos cosas distintas. La guarda que impide que
# vuelvan las reglas fiscales escritas a mano en el MCP no depende del SDK --lee
# el archivo-- asi que sigue activa en todos lados.
